# Ask the user to enter two grades
grade1 = float(input('Enter a grade: '))
grade2 = float(input('Enter another grade: '))

# Calculate the average of the two grades
average = (grade1 + grade2) / 2

# Show the average
print('The average of the two grades is {}'.format(average))
