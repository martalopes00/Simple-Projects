# Ask the user to enter their salary
salary = float(input('What is your salary? '))

# Calculate the salary with a 15% increase
new_salary = salary + (salary * 0.15)

# Show the result to the user
print(f'With a 15% increase, your salary is {new_salary}')
