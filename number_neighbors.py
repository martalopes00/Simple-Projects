# Ask the user to enter a number
n = int(input('Enter a number: '))

# Show the successor and predecessor of the number
print('The successor of {} is {} and the predecessor is {}'.format(n, n+1, n-1))
