# Ask the user to enter a value in meters
m = int(input('Enter a value in meters: '))

# Convert meters to centimeters
cm = m * 100

# Convert meters to millimeters
mm = m * 1000

# Show the result of the conversions
print(f'{m} meters are {cm} cm and {mm} mm')
